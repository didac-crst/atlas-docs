# Semantic Layer

Reserved for future synchronization commands beyond the current workbench.

The semantic core and classification UI live in `src/atlasdocs/` (PostgreSQL
model, seed loader, REST API, Paperless REST client, UI BFF) and `frontend/`
(React + TypeScript + Vite SPA served under `/ui`).

See [docs/architecture.md](../docs/architecture.md) and
[docs/frontend.md](../docs/frontend.md).
